# config.yaml
experiments:
  - name: experiment_none_filter
    filter:
      type: 'None'
      sampling_rate: 50
      cutoff: 5
    features:
      method: 'simple'

  - name: experiment_simple_butter
    filter:
      type: 'butter'
      sampling_rate: 50
      cutoff: 5
    features:
      method: 'simple'

  - name: experiment_simple_cheby1
    filter:
      type: 'cheby1'
      sampling_rate: 50
      cutoff: 5
    features:
      method: 'simple'
  
  - name: experiment_simple_cheby2
    filter:
      type: 'cheby2'
      sampling_rate: 50
      cutoff: 5
    features:
      method: 'simple'

  - name: experiment_simple_elliptic
    filter:
      type: 'elliptic'
      sampling_rate: 50
      cutoff: 5
    features:
      method: 'simple'